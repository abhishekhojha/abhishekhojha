---
export const prerender = true;
import { projectsData } from "../data/projects";
import Layout from "../layouts/Layout.astro";
import Contact from "../components/Contact.astro";
---

<Layout
    title="Projects | Abhishekh Ojha"
    description="Explore the complete portfolio of Abhishekh Ojha. A showcase of projects in web development, AI automation, full-stack architectures, and responsive design."
    keywords="Projects, Portfolio, Abhishekh Ojha, Web Development, React, Next.js, AI Automation, MERN Stack, Software Engineering"
    url="https://abhishekhojha.com/projects"
>
    <main class="pt-24 sm:pt-28 md:pt-36">
        <section class="container mx-auto px-4 sm:px-6">
            {/* Hero Header */}
            <div class="mb-14 sm:mb-20 md:mb-28">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 sm:gap-12 lg:gap-20 items-center">
                    {/* Left: Title block */}
                    <div>
                        <div class="flex items-center gap-3 mb-5 sm:mb-8">
                            <div class="w-6 sm:w-8 h-px bg-gray-900 dark:bg-gray-400"></div>
                            <p class="text-xs sm:text-sm font-medium uppercase tracking-[0.2em] text-gray-400 dark:text-gray-500">Portfolio</p>
                        </div>
                        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 dark:text-gray-100 tracking-tight leading-[1.05]">
                            All Projects
                        </h1>
                    </div>

                    {/* Right: Description + stats */}
                    <div class="flex flex-col gap-6 sm:gap-8 lg:pb-1">
                        <p class="text-base sm:text-lg text-gray-500 dark:text-gray-400 leading-relaxed">
                            A curated archive spanning web development,
                            AI automation, and full-stack engineering — each project
                            built with purpose and attention to craft.
                        </p>
                        <div class="grid grid-cols-3 gap-2 sm:gap-4">
                <div class="bg-gray-50 dark:bg-gray-800/50 rounded-lg sm:rounded-xl px-3 sm:px-4 py-4 sm:py-5 text-center">
                                <p class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-900 dark:text-gray-100">{projectsData.length}</p>
                                <p class="text-[10px] sm:text-[11px] uppercase tracking-wider text-gray-400 dark:text-gray-500 mt-1">Projects</p>
                            </div>
                            <div class="bg-gray-50 dark:bg-gray-800/50 rounded-lg sm:rounded-xl px-3 sm:px-4 py-4 sm:py-5 text-center">
                                <p class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-900 dark:text-gray-100">{[...new Set(projectsData.flatMap(p => p.skills))].length}</p>
                                <p class="text-[10px] sm:text-[11px] uppercase tracking-wider text-gray-400 dark:text-gray-500 mt-1">Technologies</p>
                            </div>
                            <div class="bg-gray-50 dark:bg-gray-800/50 rounded-lg sm:rounded-xl px-3 sm:px-4 py-4 sm:py-5 text-center">
                                <p class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-900 dark:text-gray-100">{projectsData.filter(p => p.demoUrl).length}</p>
                                <p class="text-[10px] sm:text-[11px] uppercase tracking-wider text-gray-400 dark:text-gray-500 mt-1">Live Sites</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex flex-col">
                {
                    projectsData.map((project, index) => (
                        <a
                            href={project.demoUrl || project.githubUrl || undefined}
                            target={project.demoUrl || project.githubUrl ? "_blank" : undefined}
                            rel={project.demoUrl || project.githubUrl ? "noopener noreferrer" : undefined}
                            class:list={[
                                "group block border-t border-gray-200 dark:border-gray-700/50 py-6 sm:py-8 md:py-10 transition-all duration-300",
                                (project.demoUrl || project.githubUrl) && "cursor-pointer",
                            ]}
                        >
                            <div class="flex flex-col sm:grid sm:grid-cols-12 gap-4 sm:gap-6 md:gap-8 sm:items-center">
                                {/* Number + Title */}
                                <div class="sm:col-span-5 flex items-start gap-3 sm:gap-4 md:gap-6">
                                    <span class="text-xs sm:text-sm font-medium text-gray-300 tabular-nums mt-1 sm:mt-1.5 select-none shrink-0">
                                        {String(index + 1).padStart(2, "0")}
                                    </span>
                                    <div class="min-w-0">
                                        <h3 class="text-lg sm:text-xl md:text-2xl font-bold text-gray-900 dark:text-gray-100 group-hover:text-[var(--brand-dark)] dark:group-hover:text-gray-300 transition-colors duration-300 leading-snug">
                                            {project.title}
                                        </h3>
                                        <div class="flex flex-wrap gap-1 sm:gap-1.5 mt-2 sm:mt-3">
                                            {project.skills.map((skill) => (
                                                <span class="px-2 sm:px-2.5 py-0.5 bg-gray-100 dark:bg-gray-800 text-gray-500 dark:text-gray-400 text-[10px] sm:text-[11px] font-medium rounded-full uppercase tracking-wider group-hover:bg-gray-900 dark:group-hover:bg-gray-600 group-hover:text-white transition-colors duration-300">
                                                    {skill}
                                                </span>
                                            ))}
                                        </div>
                                    </div>
                                </div>

                                {/* Description */}
                                <div class="sm:col-span-5 pl-7 sm:pl-0">
                                    <p class="text-sm sm:text-base text-gray-500 dark:text-gray-400 leading-relaxed line-clamp-2 sm:line-clamp-3">
                                        {project.description}
                                    </p>
                                </div>

                                {/* Links */}
                                <div class="sm:col-span-2 pl-7 sm:pl-0 flex items-center justify-start sm:justify-end gap-3">
                                    {project.demoUrl && (
                                        <span
                                            class="inline-flex items-center gap-1.5 text-sm font-semibold text-gray-400 group-hover:text-gray-900 transition-colors duration-300"
                                        >
                                            <span class="inline sm:hidden md:inline">Visit</span>
                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform duration-300"><path d="M7 7h10v10"/><path d="M7 17 17 7"/></svg>
                                        </span>
                                    )}
                                    {project.githubUrl && (
                                        <span
                                            class="inline-flex items-center text-gray-300 group-hover:text-gray-900 transition-colors duration-300"
                                        >
                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 22v-4a4.8 4.8 0 0 0-1-3.2c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"/><path d="M9 18c-4.51 2-5-2-7-2"/></svg>
                                        </span>
                                    )}
                                    {!project.demoUrl && !project.githubUrl && (
                                        <span class="text-xs text-gray-300 italic">Internal</span>
                                    )}
                                </div>
                            </div>
                        </a>
                    ))
                }
                <div class="border-t border-gray-200 dark:border-gray-700/50"></div>
            </div>
        </section>
        <Contact />
    </main>
</Layout>
